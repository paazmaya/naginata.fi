doctype xml
urlset(xmlns="http://www.sitemaps.org/schemas/sitemap/0.9")
  - each page in pages
    url
      loc 
        = 'http://naginata.fi' + page.loc
      lastmod
        = page.lastmod
      changefreq
        = page.changefreq
